machines:
  "0":
    constraints: "arch=amd64"
    series: "xenial"
services:
  monitor-tengu:
    charm: "edit on deployment"
    num_units: 1
    expose: true
    to:
      - "0"
    options:
      api-key: "edit on deployment"
      sojobo-ip: "edit on deployment"
      controller-type: "edit on deployment"
      controller: "edit on deployment"
      model: "edit on deployment"
  trusty-tengubeat:
    charm: "edit on deployment"
    options:
      api-key: "edit on deployment"
      sojobo-ip: "edit on deployment"
      model: "edit on deployment"
      controller: "edit on deployment"
      user: "edit on deployment"
      pass: "edit on deployment"
    series: "trusty"
  xenial-tengubeat:
    charm: "edit on deployment"
    options:
      api-key: "edit on deployment"
      sojobo-ip: "edit on deployment"
      model: "edit on deployment"
      controller: "edit on deployment"
      user: "edit on deployment"
      pass: "edit on deployment"
    series: "xenial"
relations:
  - - "{{ est-name }}:client"
    - "trusty-tengubeat:elasticsearch"
  - - "{{ est-name }}:client"
    - "xenial-tengubeat:elasticsearch"
